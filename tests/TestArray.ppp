program TestArray;
var
   int i, j, p;
   int arr[10];

void module inicia(int y)
  var int x
{
  x = 1;
  while (x < 11) do {
    arr[x] = arr[x - 1] + y;
    x = x + 1;
  }
}

main() {
  inicia(1);
}